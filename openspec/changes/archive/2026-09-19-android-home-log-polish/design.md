## Decisions

- Keep Home refresh as a local visual action for the low-fidelity prototype; no backend call is introduced.
- Keep the personal-important edit button as an affordance only until its filtering design is confirmed.
- Store newly entered demo logs in local Compose state and render them before the add-log button.
- Use white cards for task dispatch and review entry points while keeping existing neutral text.
