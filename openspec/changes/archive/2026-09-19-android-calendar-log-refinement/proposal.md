## Why

The prototype needs clearer date context and denser calendar guidance for the upcoming review. Monthly task bars should read as continuous spans, while the Logs page needs a usable first-pass workflow instead of only static entry cards.

## What Changes

- Add weekday labels to Home and Day dates and improve the return-to-today affordance.
- Add Home panel title separators.
- Add dashed day guides and consistent date typography to Week view.
- Render Month view tasks as continuous weekly bars with overflow count labels.
- Provide switchable Work Logs and Task Management sections with mock actions.

## Capabilities

### New Capabilities
- `calendar-log-refinement`: Date context, continuous calendar bars, and first-pass logs workflow.

### Modified Capabilities

## Impact

- Android Compose screens and mock date formatting only; no backend or persistence changes.
