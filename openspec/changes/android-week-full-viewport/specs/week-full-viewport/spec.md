## ADDED Requirements

### Requirement: Week timeline fills the available viewport
The Week gantt area SHALL occupy the full remaining space from below the date header to above the fixed bottom navigation without an internal scroll container.

#### Scenario: Open Week view
- **WHEN** the Week view is displayed
- **THEN** the task timeline expands to the available viewport and no unused fixed-height gap is reserved below it
